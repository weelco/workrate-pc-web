# Copy this file to .env.local and fill in values from your Supabase
# project settings (Project Settings -> API). The app runs on demo data
# when these are unset, so you can `npm run dev` before Supabase exists.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
